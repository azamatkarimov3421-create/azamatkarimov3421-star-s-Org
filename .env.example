# Supabase Ulanish Sozlamalari (Optional)
# supabase.com saytidan proyekt yaratib, ushbu o'zgaruvchilarni .env.local fayliga nusxalang:

VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# =========================================================================
# SUPABASE SQL EDITOR DAGI JADVAL YARATISH SKRIPTI (SQL Query):
# Supabase Dashboard -> SQL Editor ga kiring va quyidagilarni bajaring:
# =========================================================================

/*
create table game_results (
  id uuid default gen_random_uuid() primary key,
  created_at timestamp with time zone default timezone('utc'::text, now()) not null,
  white_player text not null,
  black_player text not null,
  winner text not null,
  game_mode text not null,
  total_moves integer not null,
  status text not null
);

-- Barcha foydalanuvchilarga o'qish va yozish ruxsatini berish (RLS policy):
alter table game_results enable row level security;
create policy "Public insert access" on game_results for insert with check (true);
create policy "Public read access" on game_results for select using (true);
*/
