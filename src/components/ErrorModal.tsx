// =====================================================
// NUR SHAXMAT 100 — ErrorModal (Nofaol / Yashirilgan)
// =====================================================

import React from 'react';

export interface ErrorModalProps {
  isOpen?: boolean;
  onClose?: () => void;
  onResetGame?: () => void;
}

export default function ErrorModal(_props: ErrorModalProps) {
  return null;
}
